version: 1
workspace_file: ./workspace.json
jvm_target: "21"

build:
  command: ["mybuild", "gradle"]
  gradle_args: ["--quiet"]

roots:
  - path: ./src/proj_1

  - path: ./src/proj_3

options:
  include_tests: false
  attach_sources: true
  follow_symlinks: true
